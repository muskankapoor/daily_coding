// Remove duplicates from a list or Generate all of the permutations of a string.

#include <iostream>
#include <string>
#include <algorithm> 
using std::cout;
using std::string;

// edge case: str  = 0 characters return
// range of characters
// empty string error

// 1. sort it
// 2. for loop if arr[i] == arr[i+]

string Remove(string str){

  std::sort(str.begin(), str.end());

  for (int i = 0; i < str.length(); i++){

    i
    
  }
  
  
}

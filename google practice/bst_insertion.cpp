/*

class Node {
    public:
        int data;
        Node *left;
        Node *right;
        Node(int d) {
            data = d;
            left = NULL;
            right = NULL;
        }
};

*/

1. if tree empty
2. if node greater than root to right
3. if root less than root to left 
